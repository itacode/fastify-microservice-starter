services:
  app:
    build:
      context: .
    container_name: ${CONTAINER_NAME}
    restart: always
    env_file:
      - .env
    ports:
      - '3000:3000'

networks:
  default:
    name: backend
